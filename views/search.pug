extends layouts/main
include mixins/videoBlock

block content
    .search__header
        h3 Searching for: #{searchingBy}
    .search__videos
        if videos.length===0
            h5 No Videos Found
        each item in videos
            +videoBlock({
                title:item.title,
                views:item.views,
                videoFile:item.fileUrl,
                id:item._id
            })
            //-이렇게 비디오 블록을 전달하면 key값을 이용해서 사용할 것임
            //-여기 역시 id:item.id와 id:item._id 똑같이 동작함 다만 _id가 오리지널이다. 